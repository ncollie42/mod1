#include "raylib.h"
#include "mod1.h"
#include <math.h>
#include <stdio.h>

void testAlgo(Image map) {
    int d = 20;
    double unit = (PI/2.0)/10;
    printf("%f\n", unit);
    double tmp = cos(unit);
    printf("%f\n", tmp);
}

void interpolateMap(Image map) {

}